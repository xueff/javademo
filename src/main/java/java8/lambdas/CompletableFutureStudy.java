package java8.lambdas;

import common.bean.Person;

import java.util.Currency;
import java.util.concurrent.CompletableFuture;

/**
 * @author xuefei
 * @Title: ${file_name}
 * @Package ${package_name}
 * @Description: ${todo}
 * @date 2018/10/1917:30
 */
public class CompletableFutureStudy {
    public static void main(String[] args) {

    }

//    private CompletableFuture<Person> findBestPrice(String productName) {
//        return CompletableFuture.supplyAsync(() -> priceFinder.find
//                BestPrice(productName));
//    }
//    private CompletableFuture<Double> lookupExchangeRate(Currency
//                                                                 localCurrency) {
//        return CompletableFuture.supplyAsync(() ->
//                exchangeService.lookupExchangeRate(Currency.USD, localCur
//                        rency));
//    }
}
